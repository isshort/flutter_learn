import 'package:flutter/material.dart';

class GoRegisterPage extends StatelessWidget {
  const GoRegisterPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('GoRegisterPage Title')),
    );
  }
}
