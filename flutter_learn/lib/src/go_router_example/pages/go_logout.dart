import 'package:flutter/material.dart';

class GoLogoutPage extends StatelessWidget {
  const GoLogoutPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('GoLogoutPage Title')),
    );
  }
}
