import 'package:flutter/material.dart';

class GoHome1Page extends StatelessWidget {
  const GoHome1Page({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('GoHome1Page Title')),
    );
  }
}
